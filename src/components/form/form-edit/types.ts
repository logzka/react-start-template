export interface IFormValues {
  // category?: { id: string; value: string; name: string };
  category?: string;
  name?: string;
  price?: number;
  oldPrice?: number;
  desc?: string;
  photo?: string;
}
